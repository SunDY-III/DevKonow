# DevKnow 生产环境配置
# 使用方式：
#   docker compose --env-file .env up
#
# 本文件覆盖 docker-compose.yml 中的默认资源限制（开发配置），
# 设置为适合生产负载的值。

# ==================== Docker 资源限制（生产环境高配版） ====================
BACKEND_MEM=4g
MYSQL_MEM=1g
QDRANT_MEM=1g
RABBITMQ_MEM=512m
MINIO_MEM=512m
